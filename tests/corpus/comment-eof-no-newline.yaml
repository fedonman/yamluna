# covers: a file whose last line is a comment with no line break after it.
# Appending a newline on dump is the single most common round-trip defect;
# so is losing the comment entirely because the scanner needs the break.
a: 1
b: 2
# a final comment with no newline after this line